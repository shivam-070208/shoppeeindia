model Category {
  id   String @id @default(auto()) @map("_id") @db.ObjectId()
  name String @unique
  slug String @unique

  deal Deal[] @relation("DealsCategory")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("category")
}
